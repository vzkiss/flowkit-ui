---
title: Creatable Combobox
description: A flexible input control with multiselect, autocomplete, and creatable support.
links:
  doc: https://base-ui.com/react/components/combobox
  api: https://base-ui.com/react/components/combobox#api-reference
---

import ComponentPreview from "@/components/markdown/component-preview";
import CreatableComboboxBasic from "@/examples/creatable-combobox-basic";
import CreatableComboboxDemo from "@/examples/creatable-combobox-demo";
import CreatableComboboxMultiple from "@/examples/creatable-combobox-multiple";

<ComponentPreview
  styleName="base-nova"
  name="creatable-combobox-basic"
  previewClassName="h-56"
>
  <CreatableComboboxBasic />
</ComponentPreview>

## Installation

Install the component into your project:

<InstallationTabs target="@flowkit-ui/creatable-combobox" />

## Usage

```tsx
import {
  CreatableCombobox,
  ComboboxItemCreatable,
  isCreatableItem,
} from "@/components/ui/creatable-combobox";
```

Single select supports both controlled and uncontrolled usage. Multiple select requires a controlled `value` prop — the caller owns the selected array and is responsible for adding newly created items to both `items` and `value` inside `onCreateValue`.

## Examples

### Basic

A simple string-based single select with creatable support.

<ComponentPreview
  styleName="base-nova"
  name="creatable-combobox-basic"
  previewClassName="h-56"
>
  <CreatableComboboxBasic />
</ComponentPreview>

### With objects

Object items with `id`, `value`, and `label` fields.

<ComponentPreview
  styleName="base-nova"
  name="creatable-combobox-demo"
  previewClassName="h-56"
>
  <CreatableComboboxDemo />
</ComponentPreview>

### Multiple

Multiple selection requires a controlled `value` prop.

<ComponentPreview
  styleName="base-nova"
  name="creatable-combobox-multiple"
  previewClassName="h-56"
>
  <CreatableComboboxMultiple />
</ComponentPreview>

## API Reference

`CreatableCombobox` wraps the shadcn/ui [`Combobox`](https://ui.shadcn.com/docs/components/base/combobox) with Base UI’s `Combobox.Root`.

| Prop                   | Type                        | Default             | Description                                                          |
| ---------------------- | --------------------------- | ------------------- | -------------------------------------------------------------------- |
| `onCreateValue`        | `(value: string) => void`   | —                   | Called after the dropdown closes when the user confirms a new value. |
| `createLabel`          | `(value: string) => string` | `Create "${value}"` | Label for the create option.                                         |
| `createOptionPosition` | `"first" \| "last"`         | `"first"`           | Where the create option appears in the list.                         |

`onValueChange` will never be called with a `CreatableItem` — use `onCreateValue` to handle new value creation.

All other props are passed through to the base-ui `Combobox.Root`.

- See the [Base UI docs](https://base-ui.com/react/components/combobox#api-reference) for the full API.
- Check [shadcn/ui](https://ui.shadcn.com/docs/components/base/combobox) Combobox for markup and composition.
